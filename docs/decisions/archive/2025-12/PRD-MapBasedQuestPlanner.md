# PRD: 맵 기반 퀘스트 계획 (Map-Based Quest Planner)

## 개요

### 목적
특정 맵을 선택하면 해당 맵에서 수행해야 할 모든 퀘스트 목표와 획득해야 할 아이템을 한눈에 보여주는 계획 도구

### 현재 문제점
- 맵 진입 전 해당 맵에서 할 일을 파악하기 어려움
- 퀘스트 목표가 여러 퀘스트에 분산되어 있음
- 필요 아이템 획득 위치를 별도로 찾아야 함
- 레이드 효율이 떨어짐 (여러 번 같은 맵 방문)

### 목표
1. 맵별 퀘스트 목표 통합 뷰
2. 맵별 필요 아이템 획득 위치 표시
3. 한 번의 레이드로 최대한 많은 목표 달성 지원
4. 레이드 전 체크리스트 기능

---

## 기능 요구사항

### FR-1: 맵 선택 및 개요

```
┌─────────────────────────────────────────────────────┐
│ 🗺️ 맵 선택                                          │
│ ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐        │
│ │CUST │WOODS│SHORE│INTER│RSRVE│LABS │GZ   │        │
│ │ 5   │ 3   │ 8   │ 4   │ 2   │ 1   │ 6   │        │
│ └─────┴─────┴─────┴─────┴─────┴─────┴─────┘        │
│        ↑ 해당 맵 할 일 개수                         │
└─────────────────────────────────────────────────────┘

┌─ Customs 선택됨 ────────────────────────────────────┐
│                                                     │
│ 📊 이 맵 요약                                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 퀘스트 목표: 5개 (방문 2, 아이템 2, 처치 1)     │ │
│ │ 획득 필요: 3종류 아이템                         │ │
│ │ 예상 소요: 2-3 레이드                           │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ [전체 보기]  [지도에서 보기]  [체크리스트 생성]     │
└─────────────────────────────────────────────────────┘
```

### FR-2: 퀘스트 목표 목록

```
┌─ 📋 퀘스트 목표 (Customs) ──────────────────────────┐
│                                                     │
│ 🔴 긴급 (현재 퀘스트 진행 중)                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ □ Dorms 3층 214호 방문                          │ │
│ │   Prapor - Delivery from the Past        [지도] │ │
│ ├─────────────────────────────────────────────────┤ │
│ │ □ USEC Stash에서 문서 획득                      │ │
│ │   Skier - Friend from the West Pt.1      [지도] │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ 🟡 선택 (활성화 가능한 퀘스트)                      │
│ ┌─────────────────────────────────────────────────┐ │
│ │ □ Scav 15명 처치                                │ │
│ │   Prapor - Punisher Pt.2                 [지도] │ │
│ ├─────────────────────────────────────────────────┤ │
│ │ □ Big Red 창고에 마커 설치                      │ │
│ │   Prapor - Delivery from the Past        [지도] │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ 🟢 완료됨 (최근 완료)                               │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ✓ Customs Office에서 서류 가방 획득             │ │
│ │   Therapist - Sanitary Standards         [지도] │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### FR-3: 필요 아이템 획득 가이드

```
┌─ 📦 획득 필요 아이템 (Customs) ─────────────────────┐
│                                                     │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 🔑 Marked Room Key (Dorms 314)                  │ │
│ │   필요: 1개 (FIR) | 보유: 0개                   │ │
│ │   ─────────────────────────────────────────     │ │
│ │   📍 획득 위치:                                 │ │
│ │   • Scav Boss 드롭 (낮은 확률)                  │ │
│ │   • Jacket 서치 (매우 낮은 확률)                │ │
│ │   • Flea Market 구매 권장                [지도] │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 💊 Salewa (3개 필요)                            │ │
│ │   필요: 3개 (FIR) | 보유: 1개                   │ │
│ │   ─────────────────────────────────────────     │ │
│ │   📍 획득 위치:                                 │ │
│ │   • Crack House 의료 가방                [지도] │ │
│ │   • Old Gas Station 의료실               [지도] │ │
│ │   • RUAF Roadblock 의무병                [지도] │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ [지도에 모든 위치 표시]                             │
└─────────────────────────────────────────────────────┘
```

### FR-4: 레이드 체크리스트

```
┌─ ✅ 레이드 체크리스트 (Customs) ────────────────────┐
│                                                     │
│ 📋 이번 레이드 목표                                 │
│ ┌─────────────────────────────────────────────────┐ │
│ │ □ Dorms 방문 (214호, 314호)                     │ │
│ │ □ Crack House에서 Salewa 찾기                   │ │
│ │ □ Big Red에 마커 설치                           │ │
│ │ □ Scav 5명 이상 처치                            │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ 🎒 필수 지참 아이템                                 │
│ ┌─────────────────────────────────────────────────┐ │
│ │ • Marker (마커 설치용) - 보유: ✓                │ │
│ │ • Dorms 314 Key - 보유: ✗ [구매 필요]          │ │
│ │ • Factory Exit Key - 보유: ✓                   │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ 💡 권장 루트                                        │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Spawn → Big Red → Crack House → Dorms → Extract │ │
│ │                                          [지도] │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ [체크리스트 복사]  [지도에서 루트 보기]             │
└─────────────────────────────────────────────────────┘
```

### FR-5: 지도 통합

```
┌─ 🗺️ Customs 지도 ───────────────────────────────────┐
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │                                             │   │
│  │    [1]●────────────●[2]                     │   │
│  │         ╲         ╱                         │   │
│  │          ╲       ╱                          │   │
│  │           ●[3]──●[4]────●[5]                │   │
│  │                                             │   │
│  │  [1] Big Red (마커 설치)                    │   │
│  │  [2] Crack House (Salewa)                   │   │
│  │  [3] Old Gas (의료품)                       │   │
│  │  [4] Dorms (214호, 314호)                   │   │
│  │  [5] ZB-1011 Extract                        │   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│ 범례: 🔴 퀘스트목표  🔵 아이템위치  🟢 탈출구       │
└─────────────────────────────────────────────────────┘
```

---

## 기술 요구사항

### TR-1: 데이터 모델

```csharp
public class MapQuestPlan
{
    public string MapKey { get; set; }
    public string MapName { get; set; }

    // 통계
    public int TotalObjectives { get; set; }
    public int CompletedObjectives { get; set; }
    public int PendingItems { get; set; }

    // 상세 데이터
    public List<MapQuestObjective> Objectives { get; set; }
    public List<MapItemLocation> ItemLocations { get; set; }
    public List<MapExtract> Extracts { get; set; }
}

public class MapQuestObjective
{
    public string ObjectiveId { get; set; }
    public string Description { get; set; }
    public string QuestId { get; set; }
    public string QuestName { get; set; }
    public string TraderName { get; set; }
    public ObjectiveType Type { get; set; }  // Visit, Mark, Kill, Find, etc.
    public ObjectivePriority Priority { get; set; }  // Urgent, Optional, Completed
    public bool IsCompleted { get; set; }
    public List<MapCoordinate> Locations { get; set; }
}

public enum ObjectivePriority
{
    Urgent,      // 현재 활성 퀘스트
    Optional,    // 활성화 가능
    Completed    // 완료됨
}

public class MapItemLocation
{
    public string ItemId { get; set; }
    public string ItemName { get; set; }
    public int RequiredCount { get; set; }
    public int OwnedCount { get; set; }
    public bool RequiresFir { get; set; }
    public List<SpawnLocation> SpawnLocations { get; set; }
}

public class SpawnLocation
{
    public string LocationName { get; set; }
    public string Description { get; set; }
    public MapCoordinate Coordinate { get; set; }
    public SpawnProbability Probability { get; set; }  // High, Medium, Low, VeryLow
}

public class RaidChecklist
{
    public string MapKey { get; set; }
    public DateTime CreatedAt { get; set; }
    public List<ChecklistItem> Objectives { get; set; }
    public List<ChecklistItem> RequiredItems { get; set; }
    public List<string> SuggestedRoute { get; set; }
}
```

### TR-2: 서비스 레이어

```csharp
public class MapQuestPlannerService
{
    // 의존성
    private readonly MapMarkerDbService _mapDb;
    private readonly QuestDbService _questDb;
    private readonly QuestProgressService _questProgress;
    private readonly ItemDbService _itemDb;
    private readonly ItemInventoryService _inventory;

    // 메서드
    public List<MapQuestPlan> GetAllMapPlans();
    public MapQuestPlan GetMapPlan(string mapKey);
    public List<MapQuestObjective> GetObjectivesForMap(string mapKey);
    public List<MapItemLocation> GetItemLocationsForMap(string mapKey);
    public RaidChecklist GenerateChecklist(string mapKey, List<string> selectedObjectives);
    public List<string> SuggestRoute(string mapKey, List<MapCoordinate> waypoints);

    // 통계
    public Dictionary<string, int> GetObjectiveCountByMap();
}
```

### TR-3: UI 구현

- **위치**: Map 탭 내 새로운 "계획" 모드 또는 별도 서브탭
- **XAML 구조**:
  - TabControl (맵 선택 탭)
  - ListView (목표/아이템 목록)
  - Canvas (지도 + 마커 오버레이)
  - CheckBox 기반 체크리스트

### TR-4: 지도 연동

```csharp
// 지도에 계획 마커 표시
public void ShowPlanMarkersOnMap(MapQuestPlan plan)
{
    // 목표 위치 마커 (빨강)
    foreach (var obj in plan.Objectives.Where(o => !o.IsCompleted))
    {
        AddMarker(obj.Locations, MarkerType.Objective, obj.Description);
    }

    // 아이템 위치 마커 (파랑)
    foreach (var item in plan.ItemLocations)
    {
        foreach (var spawn in item.SpawnLocations)
        {
            AddMarker(spawn.Coordinate, MarkerType.Item, item.ItemName);
        }
    }

    // 탈출구 마커 (초록)
    foreach (var extract in plan.Extracts)
    {
        AddMarker(extract.Coordinate, MarkerType.Extract, extract.Name);
    }
}
```

---

## UI/UX 요구사항

### UX-1: 맵 선택 UI

- 맵 버튼에 뱃지로 할 일 개수 표시
- 현재 선택된 맵 강조
- 할 일 없는 맵은 흐리게 표시

### UX-2: 우선순위 색상

| 우선순위 | 색상 | 의미 |
|----------|------|------|
| 긴급 | #F44336 (빨강) | 현재 활성 퀘스트 목표 |
| 선택 | #FFC107 (노랑) | 활성화 가능한 목표 |
| 완료 | #4CAF50 (녹색) | 이미 완료된 목표 |
| 아이템 | #2196F3 (파랑) | 획득 필요 아이템 |

### UX-3: 인터랙션

- **목표 클릭**: 지도에서 해당 위치 하이라이트
- **[지도] 버튼**: Map 탭으로 전환 + 줌
- **체크박스**: 레이드 중 완료 표시 (임시)
- **드래그**: 체크리스트 순서 변경

### UX-4: 체크리스트 공유

- 클립보드 복사 (텍스트 형식)
- 스크린샷 저장
- 인쇄용 레이아웃

---

## 구현 계획

### Phase 1: 데이터 레이어 (2일)
1. `MapQuestPlan` 모델 생성
2. `MapQuestPlannerService` 구현
3. 기존 DB 서비스 연동
4. 아이템 스폰 위치 데이터 구조

### Phase 2: 기본 UI (2-3일)
1. 맵 선택 UI
2. 목표 목록 뷰
3. 아이템 획득 가이드 뷰
4. 기본 체크리스트

### Phase 3: 지도 통합 (2일)
1. 계획 마커 오버레이
2. 클릭 → 지도 이동 연동
3. 루트 시각화 (선택적)

### Phase 4: 고급 기능 (1-2일)
1. 체크리스트 저장/불러오기
2. 루트 제안 알고리즘
3. 공유 기능

---

## 데이터 요구사항

### 필요한 추가 데이터

1. **아이템 스폰 위치**
   - 현재 DB에 없음
   - tarkov.dev API 또는 위키에서 수집 필요
   - 또는 사용자 기여 방식

2. **탈출구 정보**
   - 맵별 탈출구 위치
   - 조건부 탈출구 (키, 아이템 필요 등)

3. **키 요구사항**
   - 특정 위치 접근에 필요한 키
   - 사용자 보유 키 추적

---

## 성공 지표

- 레이드당 완료 목표 수 증가
- 같은 맵 반복 방문 횟수 감소
- 레이드 전 준비 시간 단축
- 사용자 만족도 향상
